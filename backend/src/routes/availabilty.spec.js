var expect = require("chai").expect;

/**
 * This API skipped many business logics, so it's meaningless to have unit test here. Because unit tests are majorly used for covering functions
 * I would prefer focus on integration test for such APIs, for example, using a sandbox db, create a record and then read if the record was created successfully
 *
 */

describe('Test /availability', () => {
  describe('POST', () => {
    it('dummy test', () => {
      expect(true).to.be.true;
    });
  });
});